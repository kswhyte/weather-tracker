const json = {
  data: {
    city: 'Denver',
    main: {
      temp: '32',
    },
    weather: [
      {
        description: 'snow',
      },
    ],
  },
};

export default function request(url) {
  return new Promise((resolve, reject) => {
    const 
  })
}
